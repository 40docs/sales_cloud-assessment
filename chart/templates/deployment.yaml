apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "assessment.fullname" . }}
  labels: {{- include "assessment.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels: {{- include "assessment.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels: {{- include "assessment.selectorLabels" . | nindent 8 }}
    spec:
      automountServiceAccountToken: false
      securityContext: {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: assessment
          image: {{ include "assessment.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: LISTEN_ADDR
              value: ":8080"
            - name: PUBLIC_URL
              value: {{ .Values.publicUrl | quote }}
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "assessment.databaseSecretName" . }}
                  key: uri
            - name: JWT_PRIVATE_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "assessment.fullname" . }}-secrets
                  key: jwtPrivateKey
            - name: ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "assessment.fullname" . }}-secrets
                  key: adminPassword
            - name: ADMIN_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ include "assessment.fullname" . }}-secrets
                  key: adminToken
            - name: IP_SALT
              valueFrom:
                secretKeyRef:
                  name: {{ include "assessment.fullname" . }}-secrets
                  key: ipSalt
          livenessProbe:
            httpGet: { path: /healthz, port: http }
            initialDelaySeconds: 5
          readinessProbe:
            httpGet: { path: /healthz, port: http }
            initialDelaySeconds: 2
          resources: {{- toYaml .Values.resources | nindent 12 }}
          securityContext: {{- toYaml .Values.securityContext | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector: {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations: {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity: {{- toYaml . | nindent 8 }}
      {{- end }}
