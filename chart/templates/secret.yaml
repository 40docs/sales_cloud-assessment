{{- if .Values.secret.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "assessment.fullname" . }}-secrets
  labels: {{- include "assessment.labels" . | nindent 4 }}
type: Opaque
stringData:
  jwtPrivateKey: {{ required "secret.jwtPrivateKey is required" .Values.secret.jwtPrivateKey | quote }}
  adminPassword: {{ required "secret.adminPassword is required" .Values.secret.adminPassword | quote }}
  adminToken: {{ .Values.secret.adminToken | quote }}
  ipSalt: {{ .Values.secret.ipSalt | quote }}
{{- end }}
