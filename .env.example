# Required: 32-byte ed25519 seed, base64. Generate with: openssl rand -base64 32
JWT_PRIVATE_KEY=

# Optional overrides (defaults are fine for local dev).
ADMIN_PASSWORD=localdev
ADMIN_TOKEN=localdev-cli-token
IP_SALT=localdev-salt
